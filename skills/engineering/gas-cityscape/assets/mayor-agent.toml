# Mayor — always-on city coordinator (agent.toml)
# Adapt: set `provider` only to override the workspace provider.
scope = "city"
wake_mode = "fresh"
work_dir = ".gc/agents/mayor"
idle_timeout = "1h"
max_active_sessions = 1
nudge = "Check mail and the context base, then act on what is waiting."
