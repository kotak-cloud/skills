# CLAUDE.md

This repo is a skills catalog (kotak-cloud's, modeled on mattpocock/skills). See `AGENTS.md` for layout and editing conventions. In short: every change to a skill ships a `.changeset/` entry, `CHANGELOG.md` is generated, and the one skill here — `gas-cityscape` — is model-invoked.
